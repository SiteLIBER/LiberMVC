@model IEnumerable<LiberMvc.Models.Agenda>
<!-- AGENDA BEGIN -->
<div id="agenda_wrapper">
    @if (Model != null && Model.Count() > 0)
    { 

        <div id="agenda">
            @{var item1 = Model.First(); }
            <h2>
                Agenda</h2>
            <div class="main_event">
                <p class="datetime">
                    <span class="day">@item1.DataInicio.ToString("dd")</span> <span class="month">@item1.DataInicio.ToString("MMM")</span>
                    <span class="year">@item1.DataInicio.ToString("yyyy")</span> <span class="hour">@item1.DataInicio.ToString("hh:mm")</span>
                </p>
                <h2>
                    <a href="#">@item1.Titulo</a>
                </h2>
                <p class="info">
                    @item1.Chamada
                </p>
                <p class="local">
                    @item1.Local
                </p>
                <p class="more">
                    <a href="@Url.Action("Details", "Agenda", new { id = item1.AgendaID, title = Url.FormatTitle(item1.Titulo) })">
                        Saiba +
                    </a>
                </p>
            </div>
            <ul class="other_events">
                @foreach (var item in Model.Skip(1))
                {
        
                    <li>
                        <p class="date">
                            <span class="day">@item1.DataInicio.ToString("dd")</span> <span class="month">@item1.DataInicio.ToString("MMM")</span>
                            <span class="year">@item1.DataInicio.ToString("yyyy")</span> 
                        </p>
                        <p class="more">
                            <a href="@Url.Action("Details", "Agenda", new { id = item.AgendaID, title = Url.FormatTitle(item.Titulo) })">
                                Saiba +
                            </a>
                        </p>
                        <h2>
                            <a href="#">@item.Titulo</a>
                        </h2>
                    </li>
                }
            </ul>
        </div>
    }
    <p class="full_agenda">
        <a href="#">Ver agenda completa</a></p>
</div>
<!-- AGENDA END -->
